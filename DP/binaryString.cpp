#include <iostream>

// bruteforce
int countBinaryStrings(int n) {
  // upon looking at the input and output for any number n it can easily be
  // oberserved that it is same as fibonacci number series
	// i.e. fib(N) = fib(N-1) + fib(N-2)
}

int main(void) {
  int n;
  std::cin >> n;

  std::cout << countBinaryStrings(n) << std::endl;

  return 0;
}